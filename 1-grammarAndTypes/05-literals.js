// Literals

// JavaScript defines two types of values: Fixed values and variable values.
// Fixed values: literals
// Variable values: variables

let days = ['Monday', 'Tuesday', 'Wednesday', 'Thursday', 'Friday'];
console.log(days.toString());

let tired = true;

let soup = {
    a: 'chicken noodle',
    b: 'tomato',
    c: 'beef and barley',
}

console.log(soup);
console.log(soup.c)
console.log(soup.b.length);

let car = "Honda";

let dec = 10.2;